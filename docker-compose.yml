services:
  postgres:
    image: postgres:17
    container_name: wave-sync-postgres
    environment:
      POSTGRES_DB: wave_sync
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: